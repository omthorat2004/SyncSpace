# Origin of the deployed API (no trailing slash, no /api suffix —
# api.service.ts already prefixes every call with /api/v1).
# Local dev default (used automatically if this is unset): http://localhost:8000
VITE_BACKEND_URL=http://localhost:8000
