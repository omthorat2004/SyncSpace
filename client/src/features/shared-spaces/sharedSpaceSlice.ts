import { createSlice } from "@reduxjs/toolkit";



interface share_space {
    space_id : number;
    
}

interface shareSpaceInitialState {
    loading:boolean;
    share_spaces:
}

const shareSpaceSlice = createSlice(
    name:"sharespace",
    
)